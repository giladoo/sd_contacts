<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <record id="gate_leave_list" model="ir.ui.view">
        <field name="name">Gate Leave</field>
        <field name="model">hr.leave</field>
            <field name="arch" type="xml">
                <list create="0">
                    <field name="employee_id"/>
                    <field name="holiday_status_id"/>
                    <field name="date_from"/>
                    <field name="date_to"/>
                    <field name="duration_display"/>
                <field name="state" widget="badge" decoration-warning="state in ('confirm','validate1')" decoration-success="state == 'validate'" decoration-danger="state == 'cancel'"/>
                </list>
            </field>
    </record>

    <record id="gate_leave_search" model="ir.ui.view">
        <field name="name">Gate Leave</field>
        <field name="model">hr.leave</field>
            <field name="arch" type="xml">
                <search>
                    <field name="employee_id"/>
                    <field name="date_from"/>
                    <field name="date_to"/>
                    <filter name="present" string="Present" domain="[('date_to', '=', False)]"/>
                    <filter name="today" string="Today"
                            domain="['&amp;', '|', '&amp;',
                            ('date_from', '&lt;=', datetime.datetime.combine(context_today(), datetime.time(0,0,0))),
                            ('date_to', '&gt;', datetime.datetime.combine(context_today(), datetime.time(0,0,0))),
                            ('date_from', '&gt;=', datetime.datetime.combine(context_today(), datetime.time(0,0,0))),
                            ('date_from', '&lt;', datetime.datetime.combine(context_today() + relativedelta(days=1), datetime.time(0,0,0))),

                            ]"
                    />
                    <filter name="yesterday" string="Tomorrow"
                            domain="['&amp;', '|', '&amp;',
                            ('date_from', '&lt;=', datetime.datetime.combine(context_today() + relativedelta(days=1), datetime.time(0,0,0))),
                            ('date_to', '&gt;', datetime.datetime.combine(context_today() + relativedelta(days=1), datetime.time(0,0,0))),

                            ('date_from', '&gt;=', datetime.datetime.combine(context_today() + relativedelta(days=1), datetime.time(0,0,0))),
                            ('date_from', '&lt;', datetime.datetime.combine(context_today() + relativedelta(days=2), datetime.time(0,0,0))),

                            ]"
                    />
                    <filter name="yesterday" string="After tomorrow"
                            domain="['&amp;', '|', '&amp;',
                            ('date_from', '&lt;=', datetime.datetime.combine(context_today() + relativedelta(days=2), datetime.time(0,0,0))),
                            ('date_to', '&gt;', datetime.datetime.combine(context_today() + relativedelta(days=2), datetime.time(0,0,0))),

                            ('date_from', '&gt;=', datetime.datetime.combine(context_today() + relativedelta(days=2), datetime.time(0,0,0))),
                            ('date_from', '&lt;', datetime.datetime.combine(context_today() + relativedelta(days=3), datetime.time(0,0,0))),

                            ]"
                    />



                </search>
            </field>
    </record>


    <record id="leave_action" model="ir.actions.act_window">
        <field name="name">Leaves</field>
        <field name="res_model">hr.leave</field>
        <field name="view_mode">list</field>
        <field name="view_id" ref="sd_contacts.gate_leave_list"/>
        <field name="search_view_id" ref="sd_contacts.gate_leave_search"/>
        <field name="context">{'search_default_today': 1}</field>

    </record>

        <menuitem name="Leaves"
                  id="leaves_menu"
                  parent="sd_contacts_security_gates_menu"
                  groups="sd_contacts.group_sd_contacts_users"
                  action="leave_action" />

</odoo>